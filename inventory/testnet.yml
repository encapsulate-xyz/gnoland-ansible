---
# maintains testnet inventory grouped by project names
all:
  vars:
    env: testnet
  children:
    gnoland:
      hosts:
        ts.validator.gnoland.testnet.encapsulate.xyz:
          type: validator
